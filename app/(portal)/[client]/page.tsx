// A client dashboard at /{client}. Renders the shared overview app, which reads the client from the route.
import OverviewApp from "../OverviewApp";
export default function Page() { return <OverviewApp />; }
